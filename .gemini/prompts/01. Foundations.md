You are acting as a Principal Platform Architect specializing in Spec-Driven Development (SDD) and GitOps. Your objective is to scaffold and initialize a local GitOps repository structure for a home server running on a Raspberry Pi 4 (4GB RAM, 480GB USB SSD drive, running Docker Compose V2).

Based on the core principles of SDD and the mandates in `GEMINI.md`, you must output the complete code blocks, configurations, and files required to initialize this platform.

**SECURITY MANDATE**: Never include secrets, passwords, or hardcoded sensitive paths. Use `${VARIABLE}` syntax exclusively.

Please generate the following structural elements:

### 1. FILE STRUCTURE GENERATION SCRIPT
Provide a copy-pasteable bash script that:
- Creates the directory tree (`specs/`, `environments/prod/`, `scripts/`, `.gemini/agents/`, `.telemetry/`).
- **MIGRATION LOGIC**: Safely moves any existing "legacy" files (e.g., root-level `docker-compose.yml`, `deploy-ansible.md`, `start-terminal-tmux.sh`) into their respective new locations (`environments/prod/` or `scripts/`).
- Initializes empty placeholder files where needed.
- Generates a production-ready `.gitignore` that protects local `.env` secrets, databases, and cache directories **but explicitly allows the `.telemetry/` directory** to be committed.

### 2. SYSTEM CONTEXT (.gemini/agents/system-context.md)
Write a comprehensive system context file. This serves as the primary system intent ledger. It must explicitly declare:
- **Hardware Baselines**: Raspberry Pi 4, 4GB RAM, 480GB SSD over USB 3.0.
- **Storage Strategy**: All persistent storage MUST use environment variables.
    - Configs: `${MOUNT_USB_DRIVE}/config/<service>`
    - Data/Media: `${MOUNT_USB_DRIVE}/data/<service>`
- **Strict Constraints**: 
    - Mandatory memory limits on containers (max 1.5GB default).
    - **Zero-Secrets Policy**: Zero hardcoded passwords or tokens.
    - Mandatory restart policies (`unless-stopped`).

### 3. AGENT PROFILE: DEVOPS ENGINEER
Define a prompt persona for "DevOps Engineer Expert". Its purpose is to:
- Read service specifications from `specs/`.
- **LEGACY REFACTORING**: Ingest migrated legacy configurations and refactor them into hardened `docker-compose.yml` files that strictly follow the `system-context.md` and `GEMINI.md` rules (e.g., replacing hardcoded paths with `${VARIABLE}`).
- Synthesize the final production configuration.

### 4. AGENT PROFILE: SRE DIAGNOSTICIAN
Define a prompt persona for "SRE Diagnostician". This agent operates in a **passive, read-only mode**. It analyzes logs and system state to map failures back to broken contracts in the specs, without ever altering code.

### 5. PROPOSED AGENTIC SKILLS
Propose 3-4 specific "Agentic Skills" (shell tools) for host interaction:
- **Validation**: Syntax/formatting checks for Compose and Env files.
- **Logging**: Safe retrieval of container logs for the Diagnostician.
- **Compliance**: Verification of variable usage and SSD mount status.

### 6. OPENTELEMETRY TELEMETRY STORAGE SPECIFICATION
Define configurations for intercepting and logging all agent executions using official OpenTelemetry GenAI Semantic Conventions.
- **STORAGE PATH**: All logs must be stored in a local `.telemetry/` directory within the repository root.
- **VERSIONING**: This directory MUST NOT be ignored by Git to maintain a versioned audit trail of agent behaviors.
- Provide `.env.telemetry.example` pointing to `.telemetry/gemini-gitops.log` or a local OTLP collector.
- Specify `telemetry-schema.md` for metadata-tagging manual prompts and system specs.

### 7. PRE-HOOK AUTOMATION SKILL: THE TELEMETRY LOGGER
Provide a shell implementation for a pre-hook agent loop that executes for every CLI call.
- Must capture `traceId`, target specs, and task parameters.
- Formats data into machine-readable JSON log records.
- Pipes output to the local versioned path: `.telemetry/gemini-gitops.log`.

### 8. SERVICE SPECIFICATIONS (specs/)
Generate the following declarative specification files in the `specs/` directory, reverse-engineered from the existing legacy stack:

#### `specs/navidrome.md`
- **IMAGE**: `deluan/navidrome:latest`
- **PORT**: `4533`
- **STORAGE**:
    - Data: `${MOUNT_USB_DRIVE}/Music/navidrome/data` -> `/data`
    - Music: `${MOUNT_USB_DRIVE}/Music` -> `/music:ro`
- **CONSTRAINTS**: Memory limit 1GB, `restart: unless-stopped`.

#### `specs/yubal.md`
- **IMAGE**: `ghcr.io/guillevc/yubal:latest`
- **PORT**: `4000:8000`
- **ENVIRONMENT**:
    - `YUBAL_SCHEDULER_CRON`: "0 0 * * *"
    - `YUBAL_TZ`: `UTC`
- **STORAGE**:
    - App Data: `${MOUNT_USB_DRIVE}/Music` -> `/app/data`
    - Config: `${MOUNT_USB_DRIVE}/config/yubal` -> `/app/config`
- **CONSTRAINTS**: Memory limit 1GB, `restart: unless-stopped`.

Ensure all file content blocks are cleanly separated and easy to copy.
