You are acting as a Principal Platform Architect specializing in Spec-Driven Development (SDD) and GitOps. Your objective is to scaffold and initialize a local GitOps repository structure for a home server running on a Raspberry Pi 4 (4GB RAM, 480GB USB SSD drive, running Docker Compose V2).

Based on the core principles of SDD, you must output the complete code blocks, configurations, and files required to initialize this platform.

Please generate the following structural elements:

### 1. FILE STRUCTURE GENERATION SCRIPT
Provide a copy-pasteable bash script that creates the directory tree, initializes empty files, and generates a production-ready `.gitignore` that protects local `.env` secrets, databases, and cache directories from being committed to GitHub.

### 2. SYSTEM CONTEXT (.gemini/system-context.md)
Write a comprehensive system context file. This serves as the primary system intent ledger. It must explicitly declare:
- Hardware baselines: Raspberry Pi 4, 4GB RAM, 480GB SSD over USB 3.0 mounted at `/mnt/storage`.
- Storage strategy: Configs to `/mnt/storage/config/<service>`, Data/Media to `/mnt/storage/data/<service>`.
- Strict constraints: Memory limits on containers (max 1.5GB), strict use of environment variables for paths and secrets, and mandatory restart policies (`unless-stopped`).

### 3. AGENT PROFILE: DEVOPS ENGINEER
Define a highly specific prompt persona profile for a subordinate agent called "DevOps Engineer Expert". This agent's sole purpose is to read service specifications from the `specs/` folder and compile them into optimized, hardened `docker-compose.yml` configurations that strictly adhere to the `system-context.md` rules.

### 4. AGENT PROFILE: SRE DIAGNOSTICIAN
Define a highly specific prompt persona profile for a subordinate agent called "SRE Diagnostician". This agent must operate in a completely passive, read-only mode. Its purpose is to ingest standard output logs, error streams, or system alerts, map them back to the broken contracts in the specs, and output root-cause analysis reports *without ever altering or writing code*.

### 5. PROPOSED AGENTIC SKILLS
Propose a set of 3-4 highly specific "Agentic Skills" or command flags (e.g., matching the Model Context Protocol or CLI tool execution structure) that these agents need to interact with the host system safely. Focus explicitly on:
- Validating syntax and formatting before commits.### 6. OPENTELEMETRY TELEMETRY STORAGE SPECIFICATION
Define the configurations and directory schemas to intercept and structured-log all agent executions (prompts, raw inputs, model outputs, and token counts). 
- Use the official OpenTelemetry GenAI Semantic Conventions (including `gen_ai.operation.name`, `gen_ai.request.model`, `gen_ai.usage.input_tokens`, and `gen_ai.usage.output_tokens`).
- Provide an environment-variable schema file (`.env.telemetry.example`) pointing to local standard JSON log streams or a local OTLP collector endpoint (`http://localhost:4317`).
- Specify a Markdown template (`telemetry-schema.md`) defining how manual prompts and system specs are metadata-tagged before logging.

### 7. PRE-HOOK AUTOMATION SKILL: THE TELEMETRY LOGGER
Provide the implementation design or shell interceptor pattern for an absolute pre-hook agent loop. This pre-hook must execute *in any condition* (success or failure) whenever the CLI is called. Its boundaries are:
- It runs *before* passing control to the DevOps Engineer or SRE Diagnostician to record the timestamp, active `traceId`, target spec files, and incoming task parameters.
- It taps into the execution stream to capture token usage or system exceptions.
- It formats the aggregated data into a machine-readable JSON log record string that matches the OpenTelemetry data model, then pipes it to a centralized local file path (`/mnt/storage/logs/gemini-gitops.log`) to prevent telemetry losses if the network drops.
- Safely fetching live system logs for the Diagnostician.
- Testing variable compliance in `.env` templates.

Ensure all file content blocks are cleanly separated and easy to copy out of the terminal.